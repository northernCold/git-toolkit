# GTK 使用指南

GTK (Git Toolkit) 是一个 Git 发版工作流自动化 CLI 工具。

## 安装

### 方式一：全局链接（推荐开发时使用）

```bash
cd packages/git-toolkit
pnpm install
pnpm build
npm link --global
```

安装完成后，可在任意 Git 仓库中使用 `gtk` 命令。

### 方式二：全局安装

```bash
cd packages/git-toolkit
pnpm install
pnpm build
npm install -g .
```

### 方式三：开发模式直接运行

```bash
cd packages/git-toolkit
pnpm install
pnpm dev release start 1.0.0
```

## 命令一览

| 命令 | 描述 |
|:---|:---|
| `gtk release start <version>` | 初始化发版分支并合并功能 |
| `gtk release pre [version]` | 部署到预生产环境 |
| `gtk release prod [version]` | 正式发版打标 |
| `gtk release finish [version]` | 完成发版并清理 |

> 💡 `[version]` 为可选参数。如果提供版本号，命令会自动切换到对应的发布分支 `p/<version>`；如果不提供，则需要确保当前已在发布分支上。

## 完整发版流程

### 1. 开始发版

```bash
gtk release start V1.2.0
```

**执行步骤：**
1. 检查工作区是否干净
2. 拉取远程最新信息 (`git fetch origin`)
3. 更新 master 分支
4. 创建发布分支 `p/V1.2.0`
5. 交互式选择要合并的功能分支
6. 依次合并选中的功能分支
7. 推送发布分支到远程

**输出示例：**
```
┌  开始发版 V1.2.0
│
◇  工作区干净 ✓
◇  远程同步完成 ✓
◇  master 分支已更新 ✓
◇  发布分支 p/V1.2.0 已创建 ✓
│
◆  选择要合并的功能分支:
│  ○ feature/login
│  ○ feature/dashboard
│  ○ fix/bug-123
│
◇  feature/login 合并成功 ✓
◇  feature/dashboard 合并成功 ✓
◇  p/V1.2.0 已推送到远程 ✓
│
└  ✓ 发版分支 p/V1.2.0 准备完成！
```

### 2. 部署预生产环境

```bash
# 方式一：在发布分支上执行
gtk release pre

# 方式二：指定版本号（自动切换到对应发布分支）
gtk release pre V1.2.0
```

> 💡 如果提供版本号，会自动切换到 `p/<version>` 分支；否则需要确保当前在发布分支上。

**执行步骤：**
1. 如果提供版本号，切换到对应的发布分支
2. 检查当前是否在发布分支
3. 切换到 `pre` 分支并拉取最新
4. 合并发布分支到 `pre`
5. 推送 `pre` 分支
6. 切回发布分支
7. 提示访问 Jenkins 构建

**输出示例：**
```
┌  部署到预生产环境
│
◇  工作区干净 ✓
◇  当前发布分支: p/V1.2.0 ✓
◇  已切换到 pre 分支 ✓
◇  pre 分支已更新 ✓
◇  p/V1.2.0 合并成功 ✓
◇  pre 分支已推送到远程 ✓
◇  已切回 p/V1.2.0 ✓
│
└  ✓ 预生产环境部署准备完成！

┌  Jenkins 构建
│
│  请访问 Jenkins 构建 pre 分支:
│  http://10.255.254.23:8080/job/popjoy/
│
│  验收通过后执行: gtk release prod
│
└
```

### 3. 正式发版打标

```bash
# 方式一：在发布分支上执行
gtk release prod

# 方式二：指定版本号（自动切换到对应发布分支）
gtk release prod V1.2.0
```

> 💡 如果提供版本号，会自动切换到 `p/<version>` 分支；否则需要确保当前在发布分支上。

**执行步骤：**
1. 如果提供版本号，切换到对应的发布分支
2. 检查当前是否在发布分支
3. 确认是否创建正式 Tag
4. 创建带注释的 Tag（版本号即为 Tag 名）
5. 推送 Tag 到远程
6. 提示访问 Jenkins 部署生产

**输出示例：**
```
┌  正式发版打标
│
◇  工作区干净 ✓
◇  当前发布分支: p/V1.2.0, 版本: V1.2.0 ✓
│
◆  确认要为版本 V1.2.0 创建正式发布 Tag 吗?
│  ● Yes / ○ No
│
◇  Tag V1.2.0 创建成功 ✓
◇  Tag V1.2.0 已推送到远程 ✓
│
└  ✓ 正式发版 V1.2.0 完成！

┌  Jenkins 构建 (生产)
│
│  请访问 Jenkins 构建 Tag V1.2.0 部署生产环境:
│  http://10.255.254.23:8080/job/popjoy/
│
│  部署成功后执行: gtk release finish
│
└
```

### 4. 完成发版

```bash
# 方式一：在发布分支上执行
gtk release finish

# 方式二：指定版本号（自动切换到对应发布分支）
gtk release finish V1.2.0
```

> 💡 如果提供版本号，会自动切换到 `p/<version>` 分支；否则需要确保当前在发布分支上。

**执行步骤：**
1. 如果提供版本号，切换到对应的发布分支
2. 切换到 master 分支并拉取最新
3. 使用 `--no-ff` 合并发布分支（保留合并节点）
4. 推送 master 分支
5. 交互式确认是否删除发布分支
6. 交互式选择要清理的功能分支

**输出示例：**
```
┌  完成发版并清理
│
◇  工作区干净 ✓
◇  当前发布分支: p/V1.2.0 ✓
◇  已切换到 master 分支 ✓
◇  master 分支已更新 ✓
◇  p/V1.2.0 合并成功 (--no-ff) ✓
◇  master 分支已推送到远程 ✓
│
◆  是否删除发布分支 p/V1.2.0?
│  ● Yes / ○ No
│
◇  本地分支 p/V1.2.0 已删除 ✓
◇  远程分支 p/V1.2.0 已删除 ✓
│
◆  选择要删除的已合并功能分支:
│  ○ feature/login
│  ○ feature/dashboard
│
◇  本地分支 feature/login 已删除 ✓
◇  远程分支 feature/login 已删除 ✓
◇  本地分支 feature/dashboard 已删除 ✓
◇  远程分支 feature/dashboard 已删除 ✓
│
└  ✓ 发版 V1.2.0 流程已完成！
```

## 异常处理

### 合并冲突

当合并过程中发生冲突时，工具会停止并提示：

```
┌  合并冲突
│
│  请手动解决冲突后执行:
│    git add .
│    git commit
│  然后重新运行此命令
│
└
```

解决步骤：
1. 手动解决冲突文件
2. `git add .`
3. `git commit`
4. 重新运行当前命令

### 工作区不干净

如果有未提交的改动，工具会拒绝执行并提示：

```
✖  工作区有未提交的改动，请先提交或暂存
```

解决步骤：
- 提交改动：`git add . && git commit -m "message"`
- 或暂存改动：`git stash`

### 分支检查失败

如果不在正确的分支上执行命令：

```
✖  当前分支 master 不是发布分支 (p/xxx)，请先切换到发布分支
```

解决步骤：
- 切换到正确的发布分支：`git checkout p/V1.2.0`

## 配置说明

### 环境变量配置

GTK 支持通过 `.env` 文件配置环境变量。

**创建配置文件：**

```bash
# 复制示例配置
cp .env.example .env

# 编辑配置
vim .env
```

**示例 `.env` 文件：**

```bash
# Jenkins 配置
JENKINS_URL=http://10.255.254.23:8080/job/popjoy/
```

**配置加载顺序：**
1. 系统环境变量（优先级最高）
2. 当前工作目录的 `.env` 文件
3. GTK 安装目录的 `.env` 文件

> 💡 如果未配置 `JENKINS_URL`，命令执行后会显示通用提示而非 Jenkins 链接。

## 常见问题

### Q: 如何查看帮助信息？

```bash
gtk --help
gtk release --help
gtk release start --help
```

### Q: 如何查看版本号？

```bash
gtk --version
```

### Q: 发布分支命名规则是什么？

发布分支使用 `p/<version>` 格式，例如 `p/V1.2.0`。

### Q: Tag 命名规则是什么？

Tag 直接使用版本号，例如 `V1.2.0`（用户输入 `gtk release start V1.2.0`）。

